# B1-1
## 과제 목표
- HTML에서 시맨틱 태그를 왜 사용하는지 또한 본인이 어떤 기준으로 구조를 설계했는지 설명할 수 있다.
- CSS에서 Flexbox와 Grid의 차이, 그리고 언제 각각을 선택해야 하는지 설명할 수 있다.
- querySelector로 DOM을 선택하고, addEventListener로 이벤트를 연결하는 흐름을 설명할 수 있다.
- 화살표 함수, 구조분해 할당, 배열 메서드(map/filter)가 왜 필요하고 어떻게 사용하는지 설명할 수 있다.
- fetch와 async/await로 비동기 데이터를 가져오고, 로딩/성공/실패 상태를 UI로 어떻게 표현했는지 설명할 수 있다.
- "하나의 기능"을 만들기 위해 이벤트 → 상태 변경 → DOM 업데이트가 어떻게 연결되는지 설명할 수 있다. (React의 상태-렌더링 흐름의 기초)
<br>

# 💡 시맨틱 태그(Semantic Tag)를 사용하는 이유 심화
> 코드를 직관적으로 파악하고(가독성) 유지보수를 쉽게 하는 기본 목적 외에, 브라우저와 시스템에 코드의 '의미'를 전달하는 두 가지 핵심 역할이 있습니다.

## 1. 웹 접근성 (Web Accessibility) 향상
* 시각 장애인이 사용하는 스크린 리더 등 보조 기기가 웹 페이지를 읽을 때 시맨틱 태그가 아주 중요한 이정표가 됩니다.
* 현재 읽고 있는 영역이 내비게이션인지, 핵심 본문인지 명확히 구분하여 사용자에게 음성으로 정확하게 안내할 수 있습니다.

## 2. 검색 엔진 최적화 (SEO)
* 구글 같은 검색 엔진 봇이 페이지를 수집할 때, 어떤 부분이 중요 콘텐츠이고 어떤 부분이 부가 정보인지 쉽게 파악할 수 있게 해줍니다.
* 모두 똑같은 `<div>`로 구성된 문서보다 `<main>`, `<article>` 등으로 구조화된 문서가 검색 결과 노출에 훨씬 유리해집니다.
---
**🎯 요약:** 
단순한 화면 분할을 넘어 '의미 부여'가 진짜 목적입니다. 개발자 본인을 위한 가독성을 넘어, 내 웹사이트를 방문하는 검색 엔진과 다양한 환경의 사용자 모두를 배려하기 위한 필수적인 약속입니다.
